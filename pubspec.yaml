name: todo_flutter_mockito
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^3.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.1

flutter:
  uses-material-design: true
